/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'cdn.dribbble.com', 'img.freepik.com', 'images.unsplash.com'],
    },
};

module.exports = nextConfig;
